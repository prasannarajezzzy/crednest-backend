# Loaded automatically by `npm run build`, ignored during `npm run dev`.
# Committed on purpose: it holds no secrets, only which delivery path to use.

# Hostinger is Apache/PHP, so production builds post to public/api/lead.php.
# The recipient mailbox is set inside that file, not here.
VITE_LEAD_MODE=php
